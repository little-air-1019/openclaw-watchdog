.DS_Store

# Local runtime output from the planned OpenClaw watchdog.
logs/
backups/

# Generated reports are written outside this repo, but keep this path ignored if
# future tests point report output into the workspace.
reports/

# Local watchdog config may contain notification endpoints.
scripts/openclaw-watchdog/watchdog.config.json
